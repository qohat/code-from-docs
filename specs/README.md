# specs/

Reserved for backlog artifacts (e.g. a JSON dump of a dry-run) if you later want
the generator to persist them. Today the backlog lives as **GitHub issues**, not
files here — see `.github/workflows/generate-backlog.yml`.

Kept in git so the folder exists; safe to leave empty.
